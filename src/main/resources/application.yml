spring:
  profiles:
    active: "local"
    include:
      - "accounts"
  jpa:
    hibernate.ddl-auto: "update"
    database: "mysql"
  datasource:
    url: "jdbc:mysql://localhost:3306/expenses"
    username: "expenses"
    password: "dummypass"
  data.rest.basePath: "/api"
management:
  endpoint:
    health:
      show-details: always
